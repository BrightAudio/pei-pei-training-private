# Pei Pei Private Training Site

This repository publishes an encrypted static wrapper for the Pei Pei training app.

The source app is stored separately in the private `pei-pei-training` repository. This published site does not include the app source in plaintext. A password is required in the browser to decrypt and run the app.

Do not commit the publishing password to this repository.
